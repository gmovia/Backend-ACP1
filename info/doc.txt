pip install uvicorn fastapi
pip freeze
pip freeze > requirements.txt


uvicorn app:app => Ejecuta el servidor
uvicorn app:app --reload => Podemos actualizar sin necesidad de estar todo el tiempo ejecutando el servidor.

Excepciones
raise HTTPException(status_code=codigo, detail=string)